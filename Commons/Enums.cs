namespace Commons;

public enum SectionTitleSize
{
    big,
    smallViewAll,
    small,
}

public enum SectionFormat
{
    multipleRowsCustomColumns,
    multipleRows,
    oneRow,
}

public enum TypeInformation
{
    track,
    artist,
    playList,
    album,
    explore,
    podcast
}

public enum CardFormat
{
    circle,
    blog,
    blogUnderline,
    basic,
    fillBackGround,
    row
}

public enum IsPlay
{
    playing,
    play,
    pause,
    ended
}
